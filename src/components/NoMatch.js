import React, { Component } from "react";

export default function () {
    return (
        <div className="no-match-wrapper">
            <div className="no-match">
                we don't do that here
                <p>or "404 page not found"</p>
            </div>
        </div>
    );
}
